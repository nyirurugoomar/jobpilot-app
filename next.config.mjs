/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['res.cloudinary.com','example.com','picsum.photos', 'www.google.com']
    }
};

export default nextConfig;
